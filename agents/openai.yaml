interface:
  display_name: "Deadline-Carl"
  short_description: "Run recoverable proof loops with frozen work-item progress."
  default_prompt: "Use $deadline-carl for this non-trivial repository task, freeze measurable work items, report implementation and verification progress separately, keep proof standards fixed, and start the recoverable supervisor only after explicit authorization."
policy:
  allow_implicit_invocation: false
