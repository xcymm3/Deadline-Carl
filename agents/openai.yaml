interface:
  display_name: "Codex Durable Loop"
  short_description: "Run proof-driven Codex tasks with checkpoints, timeouts, heartbeat, and manual recovery."
  default_prompt: "Use $codex-durable-loop for this non-trivial repository task. Initialize a repo-local task contract, start the external PowerShell supervisor only after explicit authorization, and report the task ID plus status command. Do not register scheduled tasks."
policy:
  allow_implicit_invocation: false
